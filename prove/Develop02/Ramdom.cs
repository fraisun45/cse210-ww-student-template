/*
public class Random
{
Random random = new Random();
int numberOfQuestions = questions.Count;

// Generate a random index
int randomIndex = random.Next(numberOfQuestions);

// Get a random question
string randomQuestion = questions[randomIndex];
}
*/